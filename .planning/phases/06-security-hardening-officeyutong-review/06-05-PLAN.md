---
phase: 06-security-hardening-officeyutong-review
plan: 05
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - off-chain/indexer/src/indexer.ts
  - off-chain/transaction-builder/src/builder.ts
  - off-chain/transaction-builder/src/types.ts
  - off-chain/transaction-builder/deploy-contracts.ts
autonomous: true
requirements:
  - SEC-06

must_haves:
  truths:
    - Indexer CKB client is network-aware (not hard-coded ClientPublicTestnet)
    - Transaction builder updated for new contract args (receipt args = pledge_type_hash, pledge args = receipt_type_hash)
    - Deploy script handles new args for receipt and pledge type scripts
    - All off-chain TypeScript packages compile without errors
  artifacts:
    - path: off-chain/indexer/src/indexer.ts
      provides: Network-aware CKB client initialization
      must_contain: ["network", "createCkbClient"]
    - path: off-chain/transaction-builder/src/builder.ts
      provides: Updated builder with new contract args and refund path
      must_contain: ["permissionlessRefund"]
  key_links:
    - from: indexer
      to: CKB network config
      via: env var or createCkbClient
      pattern: "NEXT_PUBLIC_NETWORK|CKB_NETWORK"

---

<objective>
Update off-chain services to work with the hardened contracts — fix indexer network client, update transaction builder for new contract args, and prepare deployment.

Purpose: The hardened contracts (Plans 01-04) introduce new args requirements (receipt type args = pledge_type_hash, pledge type args = receipt_type_hash) and change the refund path. Off-chain code must be updated to match. The indexer's hard-coded network client is also fixed.

Output: Updated indexer with network-aware client, updated builder with correct args and refund flow, deployment script ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-security-hardening-officeyutong-review/06-CONTEXT.md

Off-chain code:
@off-chain/indexer/src/indexer.ts (network client initialization)
@off-chain/transaction-builder/src/builder.ts (builder methods)
@off-chain/transaction-builder/src/types.ts (type definitions)
@off-chain/transaction-builder/deploy-contracts.ts (deployment script)
@off-chain/transaction-builder/src/ckbClient.ts (client factory)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix indexer network client (Issue 6a)</name>
  <files>
    off-chain/indexer/src/indexer.ts
  </files>
  <read_first>
    off-chain/indexer/src/indexer.ts
    off-chain/transaction-builder/src/ckbClient.ts
  </read_first>
  <action>
Find and fix any hard-coded `ClientPublicTestnet` references in the indexer:

1. Search for `ClientPublicTestnet` or similar hard-coded client creation
2. Replace with network-aware client creation using env var (e.g., `CKB_NETWORK` or `CKB_RPC_URL`)
3. Use the existing `createCkbClient` pattern from transaction-builder/src/ckbClient.ts if applicable
4. If the indexer already uses RPC URL from env var, verify it's correct and document

The indexer may use CCC's `ClientPublicTestnet` for certain operations like `getCurrentBlockNumber`. These should use the configured RPC URL instead.
  </action>
  <verify>
    automated: cd /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter && grep -rn "ClientPublicTestnet\|ClientPublicMainnet" off-chain/indexer/src/ | head -5
  </verify>
  <done>
    Indexer uses network-aware client — no hard-coded testnet references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update transaction builder for new contract args</name>
  <files>
    off-chain/transaction-builder/src/builder.ts
    off-chain/transaction-builder/src/types.ts
  </files>
  <read_first>
    off-chain/transaction-builder/src/builder.ts
    off-chain/transaction-builder/src/types.ts
  </read_first>
  <action>
Update builder methods to set correct args on new contract deployments:

1. **createPledgeWithReceipt:**
   - Set receipt type script args = pledge type script hash (32 bytes)
   - Set pledge type script args = receipt type script hash (32 bytes)
   - These hashes come from the deployment config (ContractInfo.codeHash)

2. **Verify permissionlessRefund is receipt-free:**
   - Should already be done in Plan 03 Task 2
   - Verify the change is integrated and TypeScript compiles

3. **Update deployment/builder types if needed:**
   - Ensure ContractInfo or deployment config supports the new args
   - Ensure the builder has access to both pledge and receipt code hashes for cross-referencing

4. **TypeScript compile check:**
   - `cd off-chain/transaction-builder && npx tsc --noEmit`
  </action>
  <verify>
    automated: cd /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/transaction-builder && npx tsc --noEmit 2>&1 | tail -5
  </verify>
  <done>
    Builder creates pledge/receipt cells with correct cross-referencing args.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update deploy script for hardened contracts</name>
  <files>
    off-chain/transaction-builder/deploy-contracts.ts
  </files>
  <read_first>
    off-chain/transaction-builder/deploy-contracts.ts
  </read_first>
  <action>
Verify the deployment script handles all 5 contracts (campaign, pledge, pledge-lock, receipt, campaign-lock):

1. All contracts should be compiled before deployment
2. Deployment order may matter if args reference other contract hashes:
   - Deploy campaign first (independent)
   - Deploy campaign-lock (independent)
   - Deploy pledge-lock (independent — args set at cell creation time, not deployment)
   - Deploy pledge and receipt — these need each other's code hash for args
   - Note: type script args are set when cells are created, not when contracts are deployed. The code_hash of a contract is determined at deployment time. So deploy both pledge and receipt, then use their code hashes when creating cells.
3. Update any deployment documentation or notes
4. Verify deployment script compiles
  </action>
  <verify>
    automated: cd /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/transaction-builder && npx tsc --noEmit 2>&1 | tail -5
  </verify>
  <done>
    Deploy script ready for all 5 hardened contracts.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Indexer network-aware:** No hard-coded testnet client references
2. **Builder updated:** Correct args on pledge/receipt type scripts
3. **Deploy script ready:** Handles all 5 contracts
4. **All TypeScript compiles:** indexer and transaction-builder pass tsc
</verification>

<success_criteria>
- Indexer CKB client reads network from env var
- Builder sets receipt args = pledge_type_hash and pledge args = receipt_type_hash
- permissionlessRefund doesn't use receipt as input
- Deploy script updated for all contracts
- All TypeScript packages compile
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-hardening-officeyutong-review/06-05-SUMMARY.md`
</output>
