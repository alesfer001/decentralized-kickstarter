---
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - e2e/scenario-6-v1.1-trustless-distribution.md
  - e2e/scenario-7-v1.1-receipt-display.md
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
---

# Wave 2: E2E Test Scenarios for v1.1 Frontend

Create browser-automated E2E test scenarios that validate the v1.1 trustless distribution UX. These scenarios follow the existing `e2e/` markdown pattern used with `claude --chrome` and verify that the frontend correctly shows automatic distribution status, receipt cells, and has no manual release/refund buttons.

## must_haves

- E2E scenario for trustless distribution status display (no manual buttons, status badges, aggregate status)
- E2E scenario for receipt cell display (receipt amount, explorer link)
- Scenarios follow the existing `e2e/scenario-*.md` format
- Both success and failure lifecycle paths covered

## Tasks

<task id="03-02-01" title="Create E2E scenario for v1.1 trustless distribution UX">

<read_first>
- e2e/scenario-1-successful-campaign.md
- e2e/scenario-2-failed-campaign-refund.md
- off-chain/frontend/src/app/campaigns/[id]/page.tsx
- .planning/phases/03-frontend-and-e2e-testing/03-CONTEXT.md
</read_first>

<action>
Create `e2e/scenario-6-v1.1-trustless-distribution.md` with the following content:

```markdown
# Scenario 6: v1.1 Trustless Distribution — No Manual Buttons

## Purpose
Validate that the v1.1 frontend removes manual release/refund buttons and shows automatic distribution status badges. This scenario tests requirement UI-01 (remove manual buttons, show automatic distribution status) and UI-03 (pledge lock status badges).

## Prerequisites
- Devnet running (`offckb node`)
- Indexer running with v1.1 contracts (`cd off-chain/indexer && npm run dev`)
- Frontend running (`cd off-chain/frontend && npm run dev`)
- All 4 v1.1 contracts deployed to devnet

## Prompt for `claude --chrome`

Navigate to http://localhost:3000. This is a decentralized crowdfunding app on CKB blockchain (v1.1 with trustless automatic distribution).

STEP 1 — Create a Campaign (as Account #0, the creator)
1. Click "Create Campaign".
2. Fill in:
   - Title: "Trustless Distribution Test"
   - Description: "Testing v1.1 automatic fund routing"
   - Funding Goal: 200 (CKB)
   - Deadline: current block + 20
3. Click "Create Campaign" and wait for redirect.
4. Verify: campaign page shows "Trustless Distribution Test", status "Active".

STEP 2 — Pledge (as Account #1, a backer)
1. Switch to Account #1 via the header dropdown.
2. Enter 250 in the pledge amount field and click "Pledge".
3. Wait for the pledge to appear in the pledges list.
4. Verify the pledge row shows:
   - The backer's truncated lock hash
   - A "Locked" status badge (gray background)
   - The pledge amount (250 CKB)
5. Verify: there is NO "Claim Refund" button anywhere on the page.
6. Verify: there is NO "Release to Creator" button anywhere on the page.

STEP 3 — Wait for deadline and finalize (as Account #0)
1. Switch back to Account #0.
2. Wait for the deadline to pass (refresh until status changes).
3. Once "Finalize Campaign" button appears, click it.
4. Wait for redirect to finalized campaign page.
5. Verify: status badge shows "Funded" (green).

STEP 4 — Verify automatic distribution status
1. Verify: a "Distribution Status" section is visible below the campaign details.
2. Verify: the distribution status text mentions automatic/permissionless distribution.
3. Verify: there is NO "Claim Refund" button on the page.
4. Verify: there is NO "Release to Creator" button on the page.
5. Verify: each pledge row still shows a "Locked" status badge (the pledge cell is still live on chain, awaiting permissionless release).

STEP 5 — Verify no manual action buttons after finalization
1. Switch to Account #1 (the backer).
2. Verify: the page does NOT show any "Claim Refund" or "Release to Creator" buttons.
3. Verify: the "Actions" section either does not appear or contains only system actions (no refund/release).

REPORT: For each step, report whether it passed or failed. Specifically confirm:
- "Locked" badge is visible on pledge rows
- "Distribution Status" section is visible after finalization
- No "Claim Refund" button exists anywhere
- No "Release to Creator" button exists anywhere
```
</action>

<acceptance_criteria>
- grep "Trustless Distribution" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "No Manual Buttons" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "Locked" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "Distribution Status" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "Claim Refund" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "Release to Creator" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "UI-01" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "UI-03" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
- grep "permissionless" e2e/scenario-6-v1.1-trustless-distribution.md returns a match
</acceptance_criteria>

</task>

<task id="03-02-02" title="Create E2E scenario for v1.1 receipt cell display">

<read_first>
- e2e/scenario-1-successful-campaign.md
- off-chain/frontend/src/app/campaigns/[id]/page.tsx
- off-chain/frontend/src/lib/api.ts
- .planning/phases/03-frontend-and-e2e-testing/03-CONTEXT.md
</read_first>

<action>
Create `e2e/scenario-7-v1.1-receipt-display.md` with the following content:

```markdown
# Scenario 7: v1.1 Receipt Cell Display — Proof of Pledge

## Purpose
Validate that the v1.1 frontend displays receipt cells inline with pledges, showing the receipt amount and an explorer link. This scenario tests requirement UI-02 (frontend displays receipt cells per backer as proof of pledge).

## Prerequisites
- Devnet running (`offckb node`)
- Indexer running with v1.1 contracts (`cd off-chain/indexer && npm run dev`)
- Frontend running (`cd off-chain/frontend && npm run dev`)
- All 4 v1.1 contracts deployed to devnet

## Prompt for `claude --chrome`

Navigate to http://localhost:3000. This is a decentralized crowdfunding app on CKB blockchain (v1.1 with receipt cells).

STEP 1 — Create a Campaign (as Account #0)
1. Click "Create Campaign".
2. Fill in:
   - Title: "Receipt Display Test"
   - Description: "Testing receipt cell display for proof of pledge"
   - Funding Goal: 300 (CKB)
   - Deadline: current block + 30
3. Click "Create Campaign" and wait for redirect.
4. Verify: campaign page shows "Receipt Display Test".

STEP 2 — First pledge with receipt (as Account #1)
1. Switch to Account #1 via the header dropdown.
2. Enter 150 in the pledge amount field and click "Pledge".
3. Wait for the pledge to appear in the pledges list.
4. Verify the pledge row shows:
   - Amount: 150 CKB
   - A "Receipt:" label with the amount (150 CKB)
   - If on testnet/mainnet: a "View on Explorer" link is present
5. Note: On devnet, the explorer link may not appear (no explorer URL configured for devnet).

STEP 3 — Second pledge (still as Account #1)
1. Enter 100 in the pledge amount field and click "Pledge".
2. Wait for the pledge to appear.
3. Verify: there are now 2 pledges in the list.
4. Verify: the new pledge row also shows a receipt with "100 CKB".
5. Verify: both pledges have receipt information displayed inline.

STEP 4 — Verify receipt data accuracy
1. Check that each pledge's receipt amount matches its pledge amount.
2. Verify: receipt information is visible without clicking any expand/tab buttons (inline display per D-05).
3. Verify: the receipt data includes the receipt amount in CKB.

STEP 5 — Check multiple backers (optional, if time permits)
1. If another test account is available, switch to it and make a pledge.
2. Verify: the new backer's pledge also shows its own receipt information.
3. Verify: receipts are correctly matched to their respective backers.

REPORT: For each step, report whether it passed or failed. Specifically confirm:
- Receipt information appears inline with each pledge row
- Receipt amounts match pledge amounts
- "View on Explorer" link is present (testnet/mainnet) or absent (devnet)
- No separate tab or page is needed to see receipt info
```
</action>

<acceptance_criteria>
- grep "Receipt Cell Display" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "Proof of Pledge" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "Receipt:" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "View on Explorer" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "UI-02" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "receipt amount" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "inline" e2e/scenario-7-v1.1-receipt-display.md returns a match
- grep "claude --chrome" e2e/scenario-7-v1.1-receipt-display.md returns a match
</acceptance_criteria>

</task>
