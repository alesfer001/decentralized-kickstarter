---
phase: 05-permissionless-finalization-campaign-lock-script
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - off-chain/frontend/src/app/campaigns/[id]/page.tsx
  - off-chain/frontend/src/lib/constants.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - Finalize button is visible to all users (not just creator) when campaign is expired and active
    - Frontend imports and uses campaign-lock contract code hash from constants
    - isCreator check removed from finalization button visibility logic
    - Button label clearly indicates permissionless finalization
  artifacts:
    - path: off-chain/frontend/src/app/campaigns/[id]/page.tsx
      provides: Campaign detail page with updated finalization UI
      must_contain: ["Finalize", "currentBlock >= campaign.deadlineBlock"]
    - path: off-chain/frontend/src/lib/constants.ts
      provides: Campaign-lock contract code hash exported
      must_contain: ["campaignLock"]
  key_links:
    - from: campaigns/[id]/page.tsx
      to: finalization button visibility
      via: currentBlock >= deadlineBlock check (not isCreator)
      pattern: "campaign.deadlineBlock"
    - from: constants.ts
      to: campaign-lock code hash
      via: deployment config
      pattern: "campaignLock"

---

<objective>
Remove creator-only restriction from finalize button on campaign detail page and update frontend to use campaign-lock contract reference.

Purpose: Make finalization permissionless by allowing any user to finalize an expired campaign. Remove isCreator check that previously blocked non-creators.

Output: Updated campaign detail page that shows "Finalize" button to all users on expired campaigns (not just creator), frontend ready for testnet E2E testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-permissionless-finalization-campaign-lock-script/05-CONTEXT.md
@.planning/phases/05-permissionless-finalization-campaign-lock-script/05-RESEARCH.md

Frontend reference (current code):
@off-chain/frontend/src/app/campaigns/[id]/page.tsx (lines 758, 1098, 1121 with isCreator checks)
@off-chain/frontend/src/lib/constants.ts (contract code hashes and deployment info)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add campaign-lock code hash to frontend constants</name>
  <files>
    off-chain/frontend/src/lib/constants.ts
  </files>
  <read_first>
    off-chain/frontend/src/lib/constants.ts
    deployment/deployed-contracts-devnet.json
  </read_first>
  <action>
Update `off-chain/frontend/src/lib/constants.ts` to include campaign-lock contract reference:

1. **Locate CONTRACTS object in constants.ts:**
   - Should contain campaign, pledge, pledgeLock, receipt contract info
   - Each has codeHash, hashType, txHash, index

2. **Add campaignLock entry:**
   - Pattern (example):
     ```typescript
     export const CONTRACTS: Record<string, ContractInfo> = {
       campaign: { codeHash: "0x...", hashType: "data", txHash: "0x...", index: 0 },
       // ... other contracts ...
       campaignLock: {
         codeHash: process.env.NEXT_PUBLIC_CAMPAIGN_LOCK_CODE_HASH || "0x...",
         hashType: "data",
         txHash: process.env.NEXT_PUBLIC_CAMPAIGN_LOCK_TX_HASH || "0x...",
         index: 0,
       },
     };
     ```

3. **Alternatively, if CONTRACTS is built from environment variables:**
   - Add new env vars or import from deployment config
   - Example: `import deployedContracts from '../../../deployment/deployed-contracts-devnet.json'`
   - Use `deployedContracts.campaignLock.codeHash` etc.

4. **Verify types are consistent:**
   - Campaign-lock should have same ContractInfo type as other contracts
   - Check types.ts if ContractInfo is defined there
   - hashType should be "data" (same as other contracts in project)

5. **No functionality change needed in constants.ts — just add the data export:**
   - This allows campaign detail page to access the campaign-lock code hash (if needed)
   - Primarily for consistency; transaction builder on backend does the real work
  </action>
  <verify>
    automated: grep -n "campaignLock" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend/src/lib/constants.ts | head -3
  </verify>
  <done>
    constants.ts includes campaignLock entry in CONTRACTS object with codeHash, hashType, txHash, index exported
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove isCreator check from finalize button visibility</name>
  <files>
    off-chain/frontend/src/app/campaigns/[id]/page.tsx
  </files>
  <read_first>
    off-chain/frontend/src/app/campaigns/[id]/page.tsx
  </read_first>
  <action>
Update the campaign detail page to show finalize button to all users, not just creators:

**Current code pattern (from line 758, 1098, 1121):**
```typescript
// Current (creator-only):
const isCreator = walletLockHash !== null && campaign.creator.toLowerCase() === walletLockHash.toLowerCase();
if (signer && (needsFinalization || (isCreator && campaign.status !== CampaignStatus.Active && pledges.length === 0)))

// Finalize button:
{isCreator ? (
  <button onClick={handleFinalize}>Finalize Campaign</button>
) : null}
```

**New code (permissionless):**
```typescript
// Check if campaign is expired and can be finalized (not creator-specific)
const isExpired = currentBlock !== null && campaign !== null && currentBlock >= campaign.deadlineBlock;
const canFinalize = isExpired && campaign.status === CampaignStatus.Active;

// Finalize button:
{signer && canFinalize && (
  <button onClick={handleFinalize}>Finalize Campaign</button>
)}
```

**Steps:**
1. Find the line defining `isCreator` variable (around line 758)
2. Delete or comment out the isCreator definition
3. Add new variable definition for `isExpired` and `canFinalize`:
   - `isExpired = currentBlock !== null && campaign !== null && currentBlock >= campaign.deadlineBlock`
   - `canFinalize = isExpired && campaign.status === CampaignStatus.Active`

4. Locate all finalize button renderings (typically in JSX render section):
   - Should be around lines 1098, 1121 or in a button conditional block
   - Pattern: `{isCreator ? <button>...` or `{isCreator && <button>...`
   - Replace with: `{signer && canFinalize && <button>...`
   - Update button label from "Finalize Campaign" to "Finalize Campaign" (can optionally add "(Anyone)" for clarity, per RESEARCH.md line 435)

5. Remove any other isCreator references related to finalization in the same file:
   - Search for `isCreator` in campaigns/[id]/page.tsx
   - Keep any other isCreator logic (e.g., for other creator-only actions), only remove finalization-related ones

6. Verify the logic:
   - Button appears if: signer exists AND campaign is expired AND campaign status is Active
   - Button does NOT appear if: signer is missing OR campaign is not expired OR campaign already finalized
   - No creator check

7. Ensure currentBlock and campaign.deadlineBlock are available:
   - currentBlock should be loaded from fetchBlockNumber() (already in code)
   - campaign.deadlineBlock should exist on Campaign type from backend
   - If either missing, type system will flag error
  </action>
  <verify>
    automated: grep -c "isCreator" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend/src/app/campaigns/[id]/page.tsx && echo "Checking for remaining isCreator references..." && grep -n "isExpired\|canFinalize" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend/src/app/campaigns/[id]/page.tsx | head -3
  </verify>
  <done>
    isCreator variable definition removed (or logic removed for finalization), isExpired and canFinalize variables added, finalize button visibility changed to check isExpired && canFinalize instead of isCreator, button will show to all users when campaign is expired and active, no creator restriction
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript compilation and no visual regressions</name>
  <files>
    off-chain/frontend/src/app/campaigns/[id]/page.tsx
  </files>
  <read_first>
    off-chain/frontend/src/app/campaigns/[id]/page.tsx
    off-chain/frontend/src/lib/types.ts
  </read_first>
  <action>
Verify frontend compiles and Campaign type has deadlineBlock field:

1. **Check Campaign type in types.ts:**
   - Ensure Campaign interface includes deadlineBlock: number or bigint
   - Should be present (used in other parts of app)
   - If missing, type system will catch error

2. **Compile frontend to check for TypeScript errors:**
   - Run: `cd /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend && npm run build` or `next build`
   - Check for any "Type 'Campaign' has no property 'deadlineBlock'" errors
   - Should compile cleanly

3. **Search for any other isCreator finalization references:**
   - `grep -n "isCreator.*Finalize\|Finalize.*isCreator" off-chain/frontend/src/app/campaigns/[id]/page.tsx`
   - Should return no results
   - Ensures all creator checks for finalization are removed

4. **Visual check (will be done in Plan 05):**
   - Finalize button position and styling unchanged
   - Button text updated if desired ("Finalize Campaign" or "Finalize Campaign (Anyone)")
   - No layout shifts when button appears/disappears

No code changes in this task; just verification that previous tasks are complete and correct.
  </action>
  <verify>
    automated: cd /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend && npm run build 2>&1 | grep -E "(✓ Ready|error TS)" | head -5
  </verify>
  <done>
    Frontend TypeScript compiles without errors, Campaign type includes deadlineBlock field, isCreator references for finalization removed, ready for testnet E2E in Plan 05
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Frontend constants updated:**
   - `off-chain/frontend/src/lib/constants.ts` exports campaignLock in CONTRACTS object
   - Code hash, hashType, txHash, index present

2. **Finalize button permissionless:**
   - `off-chain/frontend/src/app/campaigns/[id]/page.tsx` removed isCreator check for finalization
   - Button visible when: signer exists AND currentBlock >= campaign.deadlineBlock AND campaign.status === Active
   - Button hidden when: signer missing OR campaign not expired OR campaign already finalized

3. **No TypeScript errors:**
   - `npm run build` in frontend directory succeeds
   - Campaign type includes deadlineBlock field
   - isExpired and canFinalize variables properly typed

4. **Ready for testing:**
   - Frontend can render finalize button to any user
   - Button correctly shows/hides based on deadline, not creator status
</verification>

<success_criteria>
- campaign-lock contract code hash added to constants.ts CONTRACTS object
- Frontend finalize button visibility no longer checks isCreator
- Button shows when campaign is expired (currentBlock >= deadlineBlock) and active, regardless of who the caller is
- Button hidden when campaign not expired or already finalized
- Frontend TypeScript compiles without errors
- Campaign.deadlineBlock exists on Campaign type
- Ready for manual testing on devnet (Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/05-permissionless-finalization-campaign-lock-script/05-04-SUMMARY.md` documenting:
- Frontend constants updated with campaign-lock code hash
- isCreator check removed from finalize button visibility
- Button now shows to all users when campaign is expired
- Frontend compiles without errors
- Ready for devnet testing in Plan 05
</output>
