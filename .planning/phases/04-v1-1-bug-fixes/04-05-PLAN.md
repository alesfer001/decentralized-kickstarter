---
phase: 04-v1-1-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - off-chain/frontend/src/app/campaigns/[id]/page.tsx
  - docs/IMPLEMENTATION-NOTES.md
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "Campaign detail page shows 'Finalize Campaign' button to all users (not just creator)"
    - "OR: Clear documentation explaining why permissionless finalization requires contract changes"
    - "Current workaround: Creator can still finalize via existing button (visible to creator only)"
    - "Contract-level constraint documented for future v1.2+ planning"
  artifacts:
    - path: "off-chain/frontend/src/app/campaigns/[id]/page.tsx"
      provides: "Updated finalization button visibility logic"
      min_lines: 10
    - path: "docs/IMPLEMENTATION-NOTES.md"
      provides: "Documentation of BUG-01 status, constraint analysis, and v1.2 approach"
      min_lines: 30
  key_links:
    - from: "off-chain/frontend/src/app/campaigns/[id]/page.tsx"
      to: "Campaign contract lock script behavior"
      via: "finalizeCampaign button visibility based on contract constraint"
      pattern: "isCreator.*finalizeCampaign|needsFinalization"
---

<objective>
Address permissionless finalization constraint (BUG-01).

Purpose: Campaign cells are locked with the creator's lock script, which means only the creator can spend/update them. This blocks permissionless finalization. Full fix requires contract changes (custom campaign lock script), but this is a v1.2+ item. For v1.1, establish the limitation and document the approach.

Output: Frontend clarifies the constraint (creator-only finalization for now), documentation explains v1.2 approach (custom campaign lock or alternative mechanism).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/04-CONTEXT.md
@.planning/REQUIREMENTS.md (BUG-01 definition)

## Key Decisions (Locked)
From CONTEXT.md:
- **D-11**: The root cause is that the campaign cell is locked with the creator's lock script — only the creator can spend/update it. This is a contract-level constraint.
- **D-12**: Approach: Modify `finalizeCampaign()` in the transaction builder to use a different mechanism — the campaign cell's lock must allow anyone to finalize after deadline. This likely requires deploying a custom "campaign lock" script that allows permissionless spending when transitioning from Active→Success/Failed after deadline, OR using an alternative approach like placing the campaign cell under a permissionless lock with the type script enforcing valid transitions.
- **D-13**: If contract changes are too invasive for a bug-fix phase, document the limitation and add a "Finalize" button that's visible only to the campaign creator as a temporary workaround (already partially in place).

## Canonical References
- `contracts/campaign/src/main.rs` — Campaign type script (finalization validation at lines 138-174)
- `off-chain/frontend/src/app/campaigns/[id]/page.tsx` — Finalization button logic (lines 815-831)
- Memory reference: Discussion in 04-CONTEXT.md indicates this is out-of-scope for v1.1

## Current State
- Campaign cell is locked with creator's lock script
- Only creator can call finalizeCampaign (CCC wallet must match creator)
- No permissionless finalization possible without contract redeploy
- Frontend currently shows "Finalize Campaign" button only to creator (isCreator check at line 800)
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Checkpoint: BUG-01 Scope Decision</name>
  <decision>
How to handle permissionless finalization (BUG-01) in v1.1?

**Option A: Defer to v1.2** (Recommended for v1.1 bug-fix phase)
- Leave campaign cell locked with creator lock script
- Keep frontend showing "Finalize Campaign" button only to creator
- Document the limitation and v1.2 approach in docs
- Rationale: Contract changes are invasive, redeployment needed, out-of-scope for bug-fix
- Cost: Campaigns can only be finalized by creator (not fully permissionless)

**Option B: Implement permissionless campaign lock** (Major effort, not bug-fix)
- Create new campaign-lock contract allowing permissionless spending after deadline
- Update finalizeCampaign to use new lock instead of creator lock
- Redeploy all contracts (breaking change for deployed testnet)
- Rationale: Achieves design goal but requires new contract, testing, redeployment
- Cost: Large effort, redeployment, testing needed

**Option C: Alternative: Permissionless type script validation** (Medium effort)
- Keep campaign cell under creator lock
- Add finalization logic to type script that allows "any signer can finalize after deadline"
- Requires type script modification and redeployment
- Intermediate complexity between A and B
  </decision>
  <context>
D-11 identifies the root cause (creator lock), D-12 describes approaches (campaign lock or type script), D-13 accepts deferral as acceptable for bug-fix phase.

Phase 4 is "Bug Fixes" not "Architecture Changes". Full permissionless finalization requires new contract design.

What approach fits v1.1's scope best?
  </context>
  <how-to-verify>
    1. Read D-11, D-12, D-13 in 04-CONTEXT.md
    2. Review current campaign lock implementation in contracts/campaign/src/main.rs
    3. Estimate effort/impact of each option
    4. Decide: A (defer to v1.2), B (new campaign lock), or C (type script change)
  </how-to-verify>
  <resume-signal>
Select: option-a (defer), option-b (campaign lock), or option-c (type script)

If option-a: Continue with Task 1 (document limitation)
If option-b or option-c: Update action plan with contract changes needed
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Document BUG-01 limitation and v1.2 approach [ONLY IF DECISION = OPTION-A]</name>
  <files>docs/IMPLEMENTATION-NOTES.md</files>
  <read_first>
    - docs/IMPLEMENTATION-NOTES.md (if exists) or create new file
    - contracts/campaign/src/main.rs (lines 138-174 for finalization logic)
  </read_first>
  <action>
Create or update docs/IMPLEMENTATION-NOTES.md with a section documenting BUG-01:

```markdown
## BUG-01: Permissionless Finalization (v1.1 Limitation)

### Current Behavior (v1.1)
- Campaign cells are locked with the creator's lock script
- Only the creator can call `finalizeCampaign()`
- Non-creators attempting finalization will get a transaction rejection

### Root Cause
The campaign cell's lock script is hardcoded to the creator's lock hash when the campaign is created (see contracts/campaign/src/main.rs line [X]). CKB's UTXO model requires cell lock script to authorize all spending. Only the creator's signature can unlock the creator's lock script.

### Why Not Fixed in v1.1
Permissionless finalization requires one of:
1. **New "campaign lock" contract** — Replaces creator lock with a custom lock script that allows anyone to spend after deadline (contract change + redeployment)
2. **Type script-based validation** — Keeps creator lock but adds type script logic to allow finalization by any signer after deadline (requires type script redesign + redeployment)

Both approaches require:
- New contract code
- Testing and validation
- Redeployment to testnet (breaking change for existing campaigns)
- Integration test updates

This exceeds the "bug fix" scope of Phase 4.

### v1.2 Approach (Proposed)
Implement a custom "campaign lock" script:
- Lock code: `fn validate_campaign_finalization()`
- Allow spending if:
  - Signer = creator (backward compatible with v1.1) OR
  - Current block >= campaign deadline AND type script validates transition to Success/Failed
- Benefits: Permissionless finalization + creator still authorized
- Tradeoff: New contract code, redeployment, brief testnet downtime

Alternatively, use type script for validation and put campaign under a generic permissionless lock (requires fewer signatures).

### Workaround for v1.1
- Creators can finalize their own campaigns using the "Finalize Campaign" button
- Permissionless release/refund works (D-02, D-03 of BUG-03)
- Finalization is the only operation requiring creator

### Testing
After v1.2 implements permissionless finalization:
- [ ] Non-creator wallet can call finalizeCampaign on expired campaign
- [ ] Creator wallet still authorized (backward compatible)
- [ ] Finalization correctly transitions campaign status
- [ ] Full lifecycle works with permissionless finalization
```

Add this section to docs/IMPLEMENTATION-NOTES.md (create file if it doesn't exist). This documents the constraint and design direction for future work.
  </action>
  <verify>
    <automated>grep -n "BUG-01\|Permissionless Finalization\|campaign lock" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/docs/IMPLEMENTATION-NOTES.md</automated>
  </verify>
  <done>
Documentation of BUG-01 limitation, root cause, and v1.2 approach added to IMPLEMENTATION-NOTES.md. Clear explanation of why permissionless finalization was deferred and what v1.2 needs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update frontend finalization button with clarifying text</name>
  <files>off-chain/frontend/src/app/campaigns/[id]/page.tsx</files>
  <read_first>
    - off-chain/frontend/src/app/campaigns/[id]/page.tsx (lines 815-831 for finalization button, lines 786-796 for Distribution Status section)
  </read_first>
  <action>
Update the finalization button and/or Distribution Status section to clarify the creator-only limitation:

1. In the Distribution Status section (lines 786-796), update the explanatory text to note:
   ```
   "v1.1: Fund distribution is automatic and permissionless. Anyone can trigger release/refund transactions after finalization."
   ```
   This already correctly states that release/refund are permissionless.

2. In the Actions section where "Finalize Campaign" button appears (lines 815-831):
   - Keep button visible only to creator (isCreator check)
   - Add explanatory text ABOVE the button when it's visible only to creator:
   ```tsx
   {needsFinalization && (
     <div className="mb-4">
       <p className="text-sm text-zinc-600 dark:text-zinc-400 mb-2">
         This campaign has expired and needs to be finalized on-chain.
         {BigInt(campaign.totalPledged) >= BigInt(campaign.fundingGoal)
           ? " The funding goal was met -- it will be marked as Successful. Funds will be automatically released to the creator."
           : " The funding goal was not met -- it will be marked as Failed. Funds will be automatically refunded to backers."}
       </p>
       {isCreator ? (
         <button
           onClick={handleFinalize}
           disabled={actionLoading}
           className="w-full px-4 py-3 font-medium rounded-lg bg-purple-600 text-white hover:bg-purple-700 disabled:opacity-50 disabled:cursor-not-allowed min-h-[44px]"
         >
           {actionLoading ? "Finalizing..." : "Finalize Campaign"}
         </button>
       ) : (
         <p className="text-xs text-zinc-500 italic">
           Only the campaign creator can finalize. Please ask the creator to finalize or wait for v1.2 permissionless finalization.
         </p>
       )}
     </div>
   )}
   ```

This clearly shows non-creators that finalization is creator-only in v1.1, while not breaking the existing creator flow.
  </action>
  <verify>
    <automated>grep -n "Only the campaign creator\|v1.2" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend/src/app/campaigns/[id]/page.tsx</automated>
  </verify>
  <done>
Frontend displays clarifying message to non-creators explaining finalization is creator-only in v1.1. Doesn't break existing creator flow. Reference to v1.2 included.
  </done>
</task>

</tasks>

<verification>
1. Documentation explains BUG-01 root cause (creator lock on campaign cell)
2. Documentation outlines v1.2 approaches (campaign lock or type script redesign)
3. Frontend provides clear feedback to non-creators about limitation
4. Creator-only finalization flow still works (existing functionality preserved)
5. Users understand why permissionless finalization isn't available in v1.1
</verification>

<success_criteria>
- BUG-01 is scoped as v1.2 (defer from v1.1)
- Frontend clearly communicates creator-only limitation
- Documentation provides design direction for v1.2 implementation
- No breaking changes to existing creator flow
- Users are informed about roadmap (v1.2 permissionless finalization)
</success_criteria>

<output>
After completion, create `.planning/phases/04-v1-1-bug-fixes/04-05-SUMMARY.md` documenting:
- BUG-01 deferred to v1.2 (contract changes out-of-scope for bug-fix phase)
- Root cause: Campaign cell locked with creator's lock script
- V1.2 approach: Custom campaign lock or type script redesign + redeployment
- Frontend: Clarifying message for non-creators
- Workaround: Creator can still finalize, permissionless release/refund works
</output>
