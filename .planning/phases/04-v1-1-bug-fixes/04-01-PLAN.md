---
phase: 04-v1-1-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - off-chain/frontend/src/app/campaigns/[id]/page.tsx
  - off-chain/frontend/src/lib/utils.ts
autonomous: true
requirements: [BUG-03]
must_haves:
  truths:
    - "User can see 'Trigger Release' button on funded campaigns"
    - "User can see 'Trigger Refund' button on failed campaigns"
    - "Any connected wallet can trigger distribution (no creator restriction)"
    - "Button click submits permissionless release/refund transaction"
    - "Pledge status badges update from 'Locked' to 'Released'/'Refunded' after distribution"
  artifacts:
    - path: "off-chain/frontend/src/app/campaigns/[id]/page.tsx"
      provides: "Distribution Status section with trigger buttons"
      min_lines: 30
    - path: "off-chain/frontend/src/lib/utils.ts"
      provides: "Helper function to determine button visibility"
      exports: ["getDistributionTriggerState"]
  key_links:
    - from: "off-chain/frontend/src/app/campaigns/[id]/page.tsx (Distribution Status section)"
      to: "off-chain/transaction-builder via permissionlessRelease/permissionlessRefund"
      via: "import TransactionBuilder, call builder methods via signer"
      pattern: "builder\\.permissionlessRelease|builder\\.permissionlessRefund"
---

<objective>
Add permissionless distribution trigger buttons to the campaign detail page (BUG-03 — Critical).

Purpose: Funds are currently stuck in pledge cells after finalization. Users need UI to trigger permissionless release/refund transactions to move funds to creator/backers.

Output: Campaign detail page shows "Trigger Release" and "Trigger Refund" buttons on finalized campaigns, visible to all users. Button clicks submit transactions that distribute funds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/04-CONTEXT.md
@.planning/REQUIREMENTS.md (BUG-03 definition)

## Key Decisions (Locked)
From CONTEXT.md:
- **D-02**: Buttons visible to ALL users (permissionless) — not restricted to creator or backer
- **D-03**: Single button triggers distribution for all pledges in one transaction (batch)
- **D-04**: After triggering, update pledge status badges from "Locked" to "Released"/"Refunded" via polling or page refresh
- **D-05**: Calls existing `permissionlessRelease()` / `permissionlessRefund()` from transaction builder — methods already exist

## Canonical References
- `off-chain/frontend/src/app/campaigns/[id]/page.tsx` — Campaign detail page (lines 786-796 show current Distribution Status section)
- `off-chain/transaction-builder/src/builder.ts` — Methods `permissionlessRelease` (line 503) and `permissionlessRefund` (line 575) exist and are ready
- `off-chain/frontend/src/lib/utils.ts` — `getDistributionSummary()` function (line 205) provides distribution state

## Current State
- Distribution Status section exists (lines 786-796) showing summary text only
- No buttons to trigger distribution — funds stuck after finalization
- Transaction builder already has `permissionlessRelease()` and `permissionlessRefund()` methods ready to call
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add distribution trigger buttons to Distribution Status section</name>
  <files>off-chain/frontend/src/app/campaigns/[id]/page.tsx</files>
  <read_first>
    - off-chain/frontend/src/app/campaigns/[id]/page.tsx (lines 1-50 for imports, lines 786-796 for Distribution Status section, lines 800-848 for Actions section pattern)
    - off-chain/frontend/src/lib/types.ts (for Campaign and CampaignStatus types)
  </read_first>
  <action>
In the campaign detail page, replace the Distribution Status section (lines 786-796) with an expanded version that includes trigger buttons:

1. Keep the heading "Distribution Status" and summary text
2. Add two conditional buttons below the summary:
   - Show "Trigger Release" button if campaign.status === CampaignStatus.Success AND receiptCount > 0
   - Show "Trigger Refund" button if campaign.status === CampaignStatus.Failed AND receiptCount > 0
   - Both buttons visible to ALL connected wallets (no creator/backer check per D-02)
   - Use "px-4 py-3 font-medium rounded-lg bg-purple-600 text-white hover:bg-purple-700 disabled:opacity-50 disabled:cursor-not-allowed min-h-[44px]" styling (match Finalize button)
   - Disable button while transaction is pending (use actionLoading state)

3. Implement button click handlers:
   - "Trigger Release" calls: await builder.permissionlessRelease(signer, { campaignId: campaign.id, recipientLockHash: campaign.creatorLockHash, amount: campaign.totalPledged })
   - "Trigger Refund" calls: await builder.permissionlessRefund(signer, { campaignId: campaign.id, backerLockHash: signerLockHash, receiptId: ... })

   Note: Check the actual method signatures in off-chain/transaction-builder/src/builder.ts lines 503-575 for exact parameter names/types, then use those exact names in the calls.

4. On success, set actionTxHash and show transaction submitted feedback (lines 804-813 pattern)
5. Use try-catch with toast notification for errors (existing Toast pattern in page)
6. Preserve existing actionLoading and actionTxHash state management

Per decision D-04: After user submits transaction, polling loop should update receipt counts automatically, causing pledge badges to update from "Locked" to "Released"/"Refunded" when receipts are marked as distributed in the indexer. No explicit badge update needed — rely on existing polling.
  </action>
  <acceptance_criteria>
    - grep finds "Trigger Release\|Trigger Refund" in campaign detail page
    - Buttons are inside Distribution Status section (not elsewhere in page)
    - Button visibility conditional on campaign.status === CampaignStatus.Success/Failed (grep for conditional)
    - Buttons call builder.permissionlessRelease and builder.permissionlessRefund (grep for method calls)
    - No isCreator check on button visibility (available to all wallets per D-02)
    - actionLoading state used to disable buttons during transaction
    - actionTxHash displayed after successful click
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Trigger Release\|Trigger Refund" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend/src/app/campaigns/[id]/page.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create distribution trigger state helper function</name>
  <files>off-chain/frontend/src/lib/utils.ts</files>
  <read_first>
    - off-chain/frontend/src/lib/utils.ts (lines 205-223 for getDistributionSummary pattern, lines 144-147 for getUniqueBackerCount pattern)
    - off-chain/frontend/src/lib/types.ts (for CampaignStatus enum and types)
  </read_first>
  <action>
Add a helper function to determine which distribution trigger button should be visible:

```typescript
export function getDistributionTriggerState(
  campaignStatus: CampaignStatus,
  receiptCount: number
): { showRelease: boolean; showRefund: boolean; reasonDisabled?: string } {
  if (receiptCount === 0) {
    return { showRelease: false, showRefund: false, reasonDisabled: "No pledges to distribute" };
  }

  if (campaignStatus === CampaignStatus.Success) {
    return { showRelease: true, showRefund: false };
  }

  if (campaignStatus === CampaignStatus.Failed) {
    return { showRelease: false, showRefund: true };
  }

  return { showRelease: false, showRefund: false };
}
```

This function:
- Takes campaign status and receipt count
- Returns object with showRelease and showRefund booleans
- Provides optional reasonDisabled string for button tooltip/disabled state explanation
- Used in campaign detail page to conditionally render buttons

Add after line 223 (after getDistributionSummary function).
  </action>
  <acceptance_criteria>
    - grep finds "export function getDistributionTriggerState" in utils.ts
    - Function takes campaignStatus and receiptCount parameters
    - Function returns object with showRelease and showRefund boolean fields
    - Function logic matches decision D-02 (all wallets can trigger)
    - Function placed after getDistributionSummary (line 223+)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "export function getDistributionTriggerState" /Users/ayoublesfer/Documents/Dev/decentralized-kickstarter/off-chain/frontend/src/lib/utils.ts</automated>
  </verify>
</task>

</tasks>

<verification>
1. Campaign detail page shows "Trigger Release" button when status === Success and receiptCount > 0
2. Campaign detail page shows "Trigger Refund" button when status === Failed and receiptCount > 0
3. Buttons are visible to all connected wallets (no creator/backer restriction)
4. Clicking "Trigger Release" calls builder.permissionlessRelease with correct parameters
5. Clicking "Trigger Refund" calls builder.permissionlessRefund with correct parameters
6. Transaction submitted feedback appears after button click
7. Pledge status badges update from "Locked" to "Released"/"Refunded" after distribution completes (via polling)
8. getDistributionTriggerState helper function exists and is exported
</verification>

<success_criteria>
- BUG-03 is resolved: Trigger Release/Refund buttons visible and functional on finalized campaigns
- Any wallet can trigger distribution (permissionless, per D-02)
- Buttons call existing builder methods (permissionlessRelease/permissionlessRefund per D-05)
- Pledge badges update after distribution (via existing polling per D-04)
- No contract changes needed — uses existing builder methods
</success_criteria>

<output>
After completion, create `.planning/phases/04-v1-1-bug-fixes/04-01-SUMMARY.md` documenting:
- BUG-03 fixed: Trigger buttons added to Distribution Status section
- Button visibility logic (permissionless, all wallets)
- Integration with existing builder methods
- Pledge badge update mechanism (polling-based)
</output>
